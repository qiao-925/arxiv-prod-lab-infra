# ============================================================
#  cert-manager 参数（chart: jetstack/cert-manager v1.21.2）
# ============================================================
# cert-manager 的 CRD 不由 chart 默认安装，必须显式打开
installCRDs: true

# 实验环境单副本，省资源
replicaCount: 1
webhook:
  replicaCount: 1
cainjector:
  replicaCount: 1
